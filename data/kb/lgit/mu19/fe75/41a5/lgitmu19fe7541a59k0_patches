{"data":{"list":[{"author":"claude, nebula-overhaul session 2026-08-09","cmd":"","facet":"memory","label":"memory: The nebula library (mraiser/newbound_nebula, store v42) is fo...","new":"[\n  {\n    \"claim\": \"The nebula library (mraiser/newbound_nebula, store v42) is four controls: `nebula` carries the app UI and all 14 rust commands; `lighthouse`, `network`, `member` are popup forms only. The 14 commands split into release management (info, install_release), CA/certs (create_network, add_member), rollout (join_network executed on the remote peer via the send_*(..., target) peer proxy, members), config rendering (save_config, build_config — duplicated YAML template), and systemd control (install/uninstall/start/stop/restart_service); convert_legacy is a dead stub.\",\n    \"tags\": \"nebula,architecture,controls\",\n    \"source\": {\n      \"doc\": \"read from data/nebula @ d0bcb0a, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476509\n  }\n]\n","old":"","patch_id":"p1","time":1786292476510},{"author":"claude, nebula-overhaul session 2026-08-09","cmd":"","facet":"memory","label":"memory: Nebula key doctrine: every private key lives under runtime/ne...","new":"[\n  {\n    \"claim\": \"The nebula library (mraiser/newbound_nebula, store v42) is four controls: `nebula` carries the app UI and all 14 rust commands; `lighthouse`, `network`, `member` are popup forms only. The 14 commands split into release management (info, install_release), CA/certs (create_network, add_member), rollout (join_network executed on the remote peer via the send_*(..., target) peer proxy, members), config rendering (save_config, build_config — duplicated YAML template), and systemd control (install/uninstall/start/stop/restart_service); convert_legacy is a dead stub.\",\n    \"tags\": \"nebula,architecture,controls\",\n    \"source\": {\n      \"doc\": \"read from data/nebula @ d0bcb0a, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476509\n  },\n  {\n    \"claim\": \"Nebula key doctrine: every private key lives under runtime/nebula/networks/<name>/ (gitignored by the repo), the CA key never leaves the owning instance, and member cert+key travel only as join_network parameters over the already-encrypted peer channel. The 2023 code has one wart to fix: create_network and add_member stage ca.key/certs through the process working directory (rename/copy dance) instead of using nebula-cert's -ca-key/-ca-crt/-out-crt/-out-key flags — a crash mid-command leaves a CA key in the checkout root.\",\n    \"tags\": \"nebula,security,keys,certs\",\n    \"source\": {\n      \"doc\": \"src/nebula/nebula/{create_network,add_member}.rs @ d0bcb0a\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476511\n  }\n]\n","old":"[\n  {\n    \"claim\": \"The nebula library (mraiser/newbound_nebula, store v42) is four controls: `nebula` carries the app UI and all 14 rust commands; `lighthouse`, `network`, `member` are popup forms only. The 14 commands split into release management (info, install_release), CA/certs (create_network, add_member), rollout (join_network executed on the remote peer via the send_*(..., target) peer proxy, members), config rendering (save_config, build_config — duplicated YAML template), and systemd control (install/uninstall/start/stop/restart_service); convert_legacy is a dead stub.\",\n    \"tags\": \"nebula,architecture,controls\",\n    \"source\": {\n      \"doc\": \"read from data/nebula @ d0bcb0a, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476509\n  }\n]\n","patch_id":"p2","time":1786292476511},{"author":"claude, nebula-overhaul session 2026-08-09","cmd":"","facet":"memory","label":"memory: The nebula overhaul strategy (awaiting owner review) is docs/...","new":"[\n  {\n    \"claim\": \"The nebula library (mraiser/newbound_nebula, store v42) is four controls: `nebula` carries the app UI and all 14 rust commands; `lighthouse`, `network`, `member` are popup forms only. The 14 commands split into release management (info, install_release), CA/certs (create_network, add_member), rollout (join_network executed on the remote peer via the send_*(..., target) peer proxy, members), config rendering (save_config, build_config — duplicated YAML template), and systemd control (install/uninstall/start/stop/restart_service); convert_legacy is a dead stub.\",\n    \"tags\": \"nebula,architecture,controls\",\n    \"source\": {\n      \"doc\": \"read from data/nebula @ d0bcb0a, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476509\n  },\n  {\n    \"claim\": \"Nebula key doctrine: every private key lives under runtime/nebula/networks/<name>/ (gitignored by the repo), the CA key never leaves the owning instance, and member cert+key travel only as join_network parameters over the already-encrypted peer channel. The 2023 code has one wart to fix: create_network and add_member stage ca.key/certs through the process working directory (rename/copy dance) instead of using nebula-cert's -ca-key/-ca-crt/-out-crt/-out-key flags — a crash mid-command leaves a CA key in the checkout root.\",\n    \"tags\": \"nebula,security,keys,certs\",\n    \"source\": {\n      \"doc\": \"src/nebula/nebula/{create_network,add_member}.rs @ d0bcb0a\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476511\n  },\n  {\n    \"claim\": \"The nebula overhaul strategy (awaiting owner review) is docs/overhaul-strategy.md on branch claude/nebula-library-overhaul-2gg43z of mraiser/newbound_nebula: repackage on the newbound-agent FFI pattern (meta.json root=nebula, cargo ffi dylib, tracked generated crate), install via dev.github.import + one rebuild/restart, rewrite cert commands to use nebula-cert flags, dedupe the YAML template, drop attohttpc for system_call curl, delete the pre-split src/ tree and Java relics, fill desc everywhere, keep commands admin-only.\",\n    \"tags\": \"nebula,overhaul,strategy,packaging\",\n    \"source\": {\n      \"doc\": \"docs/overhaul-strategy.md, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476512\n  }\n]\n","old":"[\n  {\n    \"claim\": \"The nebula library (mraiser/newbound_nebula, store v42) is four controls: `nebula` carries the app UI and all 14 rust commands; `lighthouse`, `network`, `member` are popup forms only. The 14 commands split into release management (info, install_release), CA/certs (create_network, add_member), rollout (join_network executed on the remote peer via the send_*(..., target) peer proxy, members), config rendering (save_config, build_config — duplicated YAML template), and systemd control (install/uninstall/start/stop/restart_service); convert_legacy is a dead stub.\",\n    \"tags\": \"nebula,architecture,controls\",\n    \"source\": {\n      \"doc\": \"read from data/nebula @ d0bcb0a, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476509\n  },\n  {\n    \"claim\": \"Nebula key doctrine: every private key lives under runtime/nebula/networks/<name>/ (gitignored by the repo), the CA key never leaves the owning instance, and member cert+key travel only as join_network parameters over the already-encrypted peer channel. The 2023 code has one wart to fix: create_network and add_member stage ca.key/certs through the process working directory (rename/copy dance) instead of using nebula-cert's -ca-key/-ca-crt/-out-crt/-out-key flags — a crash mid-command leaves a CA key in the checkout root.\",\n    \"tags\": \"nebula,security,keys,certs\",\n    \"source\": {\n      \"doc\": \"src/nebula/nebula/{create_network,add_member}.rs @ d0bcb0a\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476511\n  }\n]\n","patch_id":"p3","time":1786292476512},{"author":"claude, nebula-overhaul session 2026-08-09","cmd":"","facet":"memory","label":"memory: The nebula overhaul executed 2026-08-09 (branch claude/nebula...","new":"[\n  {\n    \"claim\": \"The nebula library (mraiser/newbound_nebula, store v42) is four controls: `nebula` carries the app UI and all 14 rust commands; `lighthouse`, `network`, `member` are popup forms only. The 14 commands split into release management (info, install_release), CA/certs (create_network, add_member), rollout (join_network executed on the remote peer via the send_*(..., target) peer proxy, members), config rendering (save_config, build_config — duplicated YAML template), and systemd control (install/uninstall/start/stop/restart_service); convert_legacy is a dead stub.\",\n    \"tags\": \"nebula,architecture,controls\",\n    \"source\": {\n      \"doc\": \"read from data/nebula @ d0bcb0a, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476509\n  },\n  {\n    \"claim\": \"Nebula key doctrine: every private key lives under runtime/nebula/networks/<name>/ (gitignored by the repo), the CA key never leaves the owning instance, and member cert+key travel only as join_network parameters over the already-encrypted peer channel. The 2023 code has one wart to fix: create_network and add_member stage ca.key/certs through the process working directory (rename/copy dance) instead of using nebula-cert's -ca-key/-ca-crt/-out-crt/-out-key flags — a crash mid-command leaves a CA key in the checkout root.\",\n    \"tags\": \"nebula,security,keys,certs\",\n    \"source\": {\n      \"doc\": \"src/nebula/nebula/{create_network,add_member}.rs @ d0bcb0a\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476511\n  },\n  {\n    \"claim\": \"The nebula overhaul strategy (awaiting owner review) is docs/overhaul-strategy.md on branch claude/nebula-library-overhaul-2gg43z of mraiser/newbound_nebula: repackage on the newbound-agent FFI pattern (meta.json root=nebula, cargo ffi dylib, tracked generated crate), install via dev.github.import + one rebuild/restart, rewrite cert commands to use nebula-cert flags, dedupe the YAML template, drop attohttpc for system_call curl, delete the pre-split src/ tree and Java relics, fill desc everywhere, keep commands admin-only.\",\n    \"tags\": \"nebula,overhaul,strategy,packaging\",\n    \"source\": {\n      \"doc\": \"docs/overhaul-strategy.md, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476512\n  },\n  {\n    \"claim\": \"The nebula overhaul executed 2026-08-09 (branch claude/nebula-library-overhaul-2gg43z on newbound_nebula + newbound): FFI dylib packaging (root=nebula, tracked crate, no deps - attohttpc replaced by curl over system_call), all 14 commands re-authored via dev.code on a disposable (cert commands use nebula-cert -ca-key/-out flags so the CA key never leaves networks/<name>/; add_member's -group flag corrected to -groups - the old flag silently misfiled group membership; members keyed by peer name not path; one config template in save_config; systemd unit is Type=simple with Restart=on-failure), convert_legacy deleted, remove_member added behind the UI's delete button, descs everywhere, 17 orphaned Java-era records removed by reference-walk. Install story: dev.github.import + one restart + app activation, proven end to end on a fresh disposable including a real v1.9.5 release download and nebula-cert verify. NOT verified (owner hardware): live systemd ops, tun devices, real overlay traffic; publishapp intentionally not run so the owner's identity, not a scratch identity, signs the app republish.\",\n    \"tags\": \"nebula,overhaul,verification,keys\",\n    \"source\": {\n      \"doc\": \"this session's verification battery (47 checks) + import/update/remove wire tests, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786295588125\n  }\n]\n","old":"[\n  {\n    \"claim\": \"The nebula library (mraiser/newbound_nebula, store v42) is four controls: `nebula` carries the app UI and all 14 rust commands; `lighthouse`, `network`, `member` are popup forms only. The 14 commands split into release management (info, install_release), CA/certs (create_network, add_member), rollout (join_network executed on the remote peer via the send_*(..., target) peer proxy, members), config rendering (save_config, build_config — duplicated YAML template), and systemd control (install/uninstall/start/stop/restart_service); convert_legacy is a dead stub.\",\n    \"tags\": \"nebula,architecture,controls\",\n    \"source\": {\n      \"doc\": \"read from data/nebula @ d0bcb0a, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476509\n  },\n  {\n    \"claim\": \"Nebula key doctrine: every private key lives under runtime/nebula/networks/<name>/ (gitignored by the repo), the CA key never leaves the owning instance, and member cert+key travel only as join_network parameters over the already-encrypted peer channel. The 2023 code has one wart to fix: create_network and add_member stage ca.key/certs through the process working directory (rename/copy dance) instead of using nebula-cert's -ca-key/-ca-crt/-out-crt/-out-key flags — a crash mid-command leaves a CA key in the checkout root.\",\n    \"tags\": \"nebula,security,keys,certs\",\n    \"source\": {\n      \"doc\": \"src/nebula/nebula/{create_network,add_member}.rs @ d0bcb0a\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476511\n  },\n  {\n    \"claim\": \"The nebula overhaul strategy (awaiting owner review) is docs/overhaul-strategy.md on branch claude/nebula-library-overhaul-2gg43z of mraiser/newbound_nebula: repackage on the newbound-agent FFI pattern (meta.json root=nebula, cargo ffi dylib, tracked generated crate), install via dev.github.import + one rebuild/restart, rewrite cert commands to use nebula-cert flags, dedupe the YAML template, drop attohttpc for system_call curl, delete the pre-split src/ tree and Java relics, fill desc everywhere, keep commands admin-only.\",\n    \"tags\": \"nebula,overhaul,strategy,packaging\",\n    \"source\": {\n      \"doc\": \"docs/overhaul-strategy.md, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476512\n  }\n]\n","patch_id":"p4","time":1786295588126},{"author":"claude, nebula-ui-rebuild session 2026-08-10","cmd":"","facet":"memory","label":"memory: The nebula UI was rebuilt 2026-08-10 on the tokens.css kit (b...","new":"[\n  {\n    \"claim\": \"The nebula library (mraiser/newbound_nebula, store v42) is four controls: `nebula` carries the app UI and all 14 rust commands; `lighthouse`, `network`, `member` are popup forms only. The 14 commands split into release management (info, install_release), CA/certs (create_network, add_member), rollout (join_network executed on the remote peer via the send_*(..., target) peer proxy, members), config rendering (save_config, build_config — duplicated YAML template), and systemd control (install/uninstall/start/stop/restart_service); convert_legacy is a dead stub.\",\n    \"tags\": \"nebula,architecture,controls\",\n    \"source\": {\n      \"doc\": \"read from data/nebula @ d0bcb0a, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476509\n  },\n  {\n    \"claim\": \"Nebula key doctrine: every private key lives under runtime/nebula/networks/<name>/ (gitignored by the repo), the CA key never leaves the owning instance, and member cert+key travel only as join_network parameters over the already-encrypted peer channel. The 2023 code has one wart to fix: create_network and add_member stage ca.key/certs through the process working directory (rename/copy dance) instead of using nebula-cert's -ca-key/-ca-crt/-out-crt/-out-key flags — a crash mid-command leaves a CA key in the checkout root.\",\n    \"tags\": \"nebula,security,keys,certs\",\n    \"source\": {\n      \"doc\": \"src/nebula/nebula/{create_network,add_member}.rs @ d0bcb0a\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476511\n  },\n  {\n    \"claim\": \"The nebula overhaul strategy (awaiting owner review) is docs/overhaul-strategy.md on branch claude/nebula-library-overhaul-2gg43z of mraiser/newbound_nebula: repackage on the newbound-agent FFI pattern (meta.json root=nebula, cargo ffi dylib, tracked generated crate), install via dev.github.import + one rebuild/restart, rewrite cert commands to use nebula-cert flags, dedupe the YAML template, drop attohttpc for system_call curl, delete the pre-split src/ tree and Java relics, fill desc everywhere, keep commands admin-only.\",\n    \"tags\": \"nebula,overhaul,strategy,packaging\",\n    \"source\": {\n      \"doc\": \"docs/overhaul-strategy.md, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476512\n  },\n  {\n    \"claim\": \"The nebula overhaul executed 2026-08-09 (branch claude/nebula-library-overhaul-2gg43z on newbound_nebula + newbound): FFI dylib packaging (root=nebula, tracked crate, no deps - attohttpc replaced by curl over system_call), all 14 commands re-authored via dev.code on a disposable (cert commands use nebula-cert -ca-key/-out flags so the CA key never leaves networks/<name>/; add_member's -group flag corrected to -groups - the old flag silently misfiled group membership; members keyed by peer name not path; one config template in save_config; systemd unit is Type=simple with Restart=on-failure), convert_legacy deleted, remove_member added behind the UI's delete button, descs everywhere, 17 orphaned Java-era records removed by reference-walk. Install story: dev.github.import + one restart + app activation, proven end to end on a fresh disposable including a real v1.9.5 release download and nebula-cert verify. NOT verified (owner hardware): live systemd ops, tun devices, real overlay traffic; publishapp intentionally not run so the owner's identity, not a scratch identity, signs the app republish.\",\n    \"tags\": \"nebula,overhaul,verification,keys\",\n    \"source\": {\n      \"doc\": \"this session's verification battery (47 checks) + import/update/remove wire tests, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786295588125\n  },\n  {\n    \"claim\": \"The nebula UI was rebuilt 2026-08-10 on the tokens.css kit (branch claude/nebula-library-overhaul-2gg43z, ea1c503, awaiting merge): all four controls' facets re-authored via patch_control_facet on a disposable; app.ui dependency dropped (own tabs/modal/status strip); owner-vs-joined gating from info.owner; outcomes read from data.status because the exec envelope is 'ok' even for failed object-returning commands. Traps hit and worth keeping: jQuery attr(name,null) does NOT remove an attribute (use removeAttr, or [hidden] never unhides); the server caches built jsapi per process, so facet js edits need an instance restart to serve (facet html/css serve fresh); mounting app:tokens via a hidden data-control div injects the kit css globally from any classic-mount app.\",\n    \"tags\": \"nebula,ui,frontend,kit,tokens,jsapi\",\n    \"source\": {\n      \"doc\": \"live Playwright verification on the disposable, 21 checks, 2026-08-10\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786320393147\n  }\n]\n","old":"[\n  {\n    \"claim\": \"The nebula library (mraiser/newbound_nebula, store v42) is four controls: `nebula` carries the app UI and all 14 rust commands; `lighthouse`, `network`, `member` are popup forms only. The 14 commands split into release management (info, install_release), CA/certs (create_network, add_member), rollout (join_network executed on the remote peer via the send_*(..., target) peer proxy, members), config rendering (save_config, build_config — duplicated YAML template), and systemd control (install/uninstall/start/stop/restart_service); convert_legacy is a dead stub.\",\n    \"tags\": \"nebula,architecture,controls\",\n    \"source\": {\n      \"doc\": \"read from data/nebula @ d0bcb0a, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476509\n  },\n  {\n    \"claim\": \"Nebula key doctrine: every private key lives under runtime/nebula/networks/<name>/ (gitignored by the repo), the CA key never leaves the owning instance, and member cert+key travel only as join_network parameters over the already-encrypted peer channel. The 2023 code has one wart to fix: create_network and add_member stage ca.key/certs through the process working directory (rename/copy dance) instead of using nebula-cert's -ca-key/-ca-crt/-out-crt/-out-key flags — a crash mid-command leaves a CA key in the checkout root.\",\n    \"tags\": \"nebula,security,keys,certs\",\n    \"source\": {\n      \"doc\": \"src/nebula/nebula/{create_network,add_member}.rs @ d0bcb0a\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476511\n  },\n  {\n    \"claim\": \"The nebula overhaul strategy (awaiting owner review) is docs/overhaul-strategy.md on branch claude/nebula-library-overhaul-2gg43z of mraiser/newbound_nebula: repackage on the newbound-agent FFI pattern (meta.json root=nebula, cargo ffi dylib, tracked generated crate), install via dev.github.import + one rebuild/restart, rewrite cert commands to use nebula-cert flags, dedupe the YAML template, drop attohttpc for system_call curl, delete the pre-split src/ tree and Java relics, fill desc everywhere, keep commands admin-only.\",\n    \"tags\": \"nebula,overhaul,strategy,packaging\",\n    \"source\": {\n      \"doc\": \"docs/overhaul-strategy.md, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476512\n  },\n  {\n    \"claim\": \"The nebula overhaul executed 2026-08-09 (branch claude/nebula-library-overhaul-2gg43z on newbound_nebula + newbound): FFI dylib packaging (root=nebula, tracked crate, no deps - attohttpc replaced by curl over system_call), all 14 commands re-authored via dev.code on a disposable (cert commands use nebula-cert -ca-key/-out flags so the CA key never leaves networks/<name>/; add_member's -group flag corrected to -groups - the old flag silently misfiled group membership; members keyed by peer name not path; one config template in save_config; systemd unit is Type=simple with Restart=on-failure), convert_legacy deleted, remove_member added behind the UI's delete button, descs everywhere, 17 orphaned Java-era records removed by reference-walk. Install story: dev.github.import + one restart + app activation, proven end to end on a fresh disposable including a real v1.9.5 release download and nebula-cert verify. NOT verified (owner hardware): live systemd ops, tun devices, real overlay traffic; publishapp intentionally not run so the owner's identity, not a scratch identity, signs the app republish.\",\n    \"tags\": \"nebula,overhaul,verification,keys\",\n    \"source\": {\n      \"doc\": \"this session's verification battery (47 checks) + import/update/remove wire tests, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786295588125\n  }\n]\n","patch_id":"p5","time":1786320393167},{"author":"claude, nebula-ui-rebuild session 2026-08-10","cmd":"","facet":"memory","label":"memory: Install-path doctrine, per the owner (2026-08-10): the peer-t...","new":"[\n  {\n    \"claim\": \"The nebula library (mraiser/newbound_nebula, store v42) is four controls: `nebula` carries the app UI and all 14 rust commands; `lighthouse`, `network`, `member` are popup forms only. The 14 commands split into release management (info, install_release), CA/certs (create_network, add_member), rollout (join_network executed on the remote peer via the send_*(..., target) peer proxy, members), config rendering (save_config, build_config — duplicated YAML template), and systemd control (install/uninstall/start/stop/restart_service); convert_legacy is a dead stub.\",\n    \"tags\": \"nebula,architecture,controls\",\n    \"source\": {\n      \"doc\": \"read from data/nebula @ d0bcb0a, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476509\n  },\n  {\n    \"claim\": \"Nebula key doctrine: every private key lives under runtime/nebula/networks/<name>/ (gitignored by the repo), the CA key never leaves the owning instance, and member cert+key travel only as join_network parameters over the already-encrypted peer channel. The 2023 code has one wart to fix: create_network and add_member stage ca.key/certs through the process working directory (rename/copy dance) instead of using nebula-cert's -ca-key/-ca-crt/-out-crt/-out-key flags — a crash mid-command leaves a CA key in the checkout root.\",\n    \"tags\": \"nebula,security,keys,certs\",\n    \"source\": {\n      \"doc\": \"src/nebula/nebula/{create_network,add_member}.rs @ d0bcb0a\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476511\n  },\n  {\n    \"claim\": \"The nebula overhaul strategy (awaiting owner review) is docs/overhaul-strategy.md on branch claude/nebula-library-overhaul-2gg43z of mraiser/newbound_nebula: repackage on the newbound-agent FFI pattern (meta.json root=nebula, cargo ffi dylib, tracked generated crate), install via dev.github.import + one rebuild/restart, rewrite cert commands to use nebula-cert flags, dedupe the YAML template, drop attohttpc for system_call curl, delete the pre-split src/ tree and Java relics, fill desc everywhere, keep commands admin-only.\",\n    \"tags\": \"nebula,overhaul,strategy,packaging\",\n    \"source\": {\n      \"doc\": \"docs/overhaul-strategy.md, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476512\n  },\n  {\n    \"claim\": \"The nebula overhaul executed 2026-08-09 (branch claude/nebula-library-overhaul-2gg43z on newbound_nebula + newbound): FFI dylib packaging (root=nebula, tracked crate, no deps - attohttpc replaced by curl over system_call), all 14 commands re-authored via dev.code on a disposable (cert commands use nebula-cert -ca-key/-out flags so the CA key never leaves networks/<name>/; add_member's -group flag corrected to -groups - the old flag silently misfiled group membership; members keyed by peer name not path; one config template in save_config; systemd unit is Type=simple with Restart=on-failure), convert_legacy deleted, remove_member added behind the UI's delete button, descs everywhere, 17 orphaned Java-era records removed by reference-walk. Install story: dev.github.import + one restart + app activation, proven end to end on a fresh disposable including a real v1.9.5 release download and nebula-cert verify. NOT verified (owner hardware): live systemd ops, tun devices, real overlay traffic; publishapp intentionally not run so the owner's identity, not a scratch identity, signs the app republish.\",\n    \"tags\": \"nebula,overhaul,verification,keys\",\n    \"source\": {\n      \"doc\": \"this session's verification battery (47 checks) + import/update/remove wire tests, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786295588125\n  },\n  {\n    \"claim\": \"The nebula UI was rebuilt 2026-08-10 on the tokens.css kit (branch claude/nebula-library-overhaul-2gg43z, ea1c503, awaiting merge): all four controls' facets re-authored via patch_control_facet on a disposable; app.ui dependency dropped (own tabs/modal/status strip); owner-vs-joined gating from info.owner; outcomes read from data.status because the exec envelope is 'ok' even for failed object-returning commands. Traps hit and worth keeping: jQuery attr(name,null) does NOT remove an attribute (use removeAttr, or [hidden] never unhides); the server caches built jsapi per process, so facet js edits need an instance restart to serve (facet html/css serve fresh); mounting app:tokens via a hidden data-control div injects the kit css globally from any classic-mount app.\",\n    \"tags\": \"nebula,ui,frontend,kit,tokens,jsapi\",\n    \"source\": {\n      \"doc\": \"live Playwright verification on the disposable, 21 checks, 2026-08-10\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786320393147\n  },\n  {\n    \"claim\": \"Install-path doctrine, per the owner (2026-08-10): the peer-to-peer path (dev.dev.install_lib streaming over the peer channel, driven from the app's own UI) is THE install story for users' peers; dev.github.import is the developer path for the first instance only. A session initially replaced the broken P2P button with git-import guidance — wrong direction, corrected. The fix that made P2P work for FFI libraries is dev.dev.activate_lib: one shared activation step (initializer regen, in-place manifest mend for the builder's default Cargo.toml, unconditional crate build, host rebuild, 'RESTART:' answer) called by both install_lib and github.import. Verified on a P2P-shaped disposable (real dirs, no repo, no crate): activate → restart → all commands live.\",\n    \"tags\": \"nebula,p2p,install,doctrine,activate_lib\",\n    \"source\": {\n      \"doc\": \"the owner's correction in session + disposable verification, 2026-08-10\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786321383753\n  }\n]\n","old":"[\n  {\n    \"claim\": \"The nebula library (mraiser/newbound_nebula, store v42) is four controls: `nebula` carries the app UI and all 14 rust commands; `lighthouse`, `network`, `member` are popup forms only. The 14 commands split into release management (info, install_release), CA/certs (create_network, add_member), rollout (join_network executed on the remote peer via the send_*(..., target) peer proxy, members), config rendering (save_config, build_config — duplicated YAML template), and systemd control (install/uninstall/start/stop/restart_service); convert_legacy is a dead stub.\",\n    \"tags\": \"nebula,architecture,controls\",\n    \"source\": {\n      \"doc\": \"read from data/nebula @ d0bcb0a, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476509\n  },\n  {\n    \"claim\": \"Nebula key doctrine: every private key lives under runtime/nebula/networks/<name>/ (gitignored by the repo), the CA key never leaves the owning instance, and member cert+key travel only as join_network parameters over the already-encrypted peer channel. The 2023 code has one wart to fix: create_network and add_member stage ca.key/certs through the process working directory (rename/copy dance) instead of using nebula-cert's -ca-key/-ca-crt/-out-crt/-out-key flags — a crash mid-command leaves a CA key in the checkout root.\",\n    \"tags\": \"nebula,security,keys,certs\",\n    \"source\": {\n      \"doc\": \"src/nebula/nebula/{create_network,add_member}.rs @ d0bcb0a\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476511\n  },\n  {\n    \"claim\": \"The nebula overhaul strategy (awaiting owner review) is docs/overhaul-strategy.md on branch claude/nebula-library-overhaul-2gg43z of mraiser/newbound_nebula: repackage on the newbound-agent FFI pattern (meta.json root=nebula, cargo ffi dylib, tracked generated crate), install via dev.github.import + one rebuild/restart, rewrite cert commands to use nebula-cert flags, dedupe the YAML template, drop attohttpc for system_call curl, delete the pre-split src/ tree and Java relics, fill desc everywhere, keep commands admin-only.\",\n    \"tags\": \"nebula,overhaul,strategy,packaging\",\n    \"source\": {\n      \"doc\": \"docs/overhaul-strategy.md, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476512\n  },\n  {\n    \"claim\": \"The nebula overhaul executed 2026-08-09 (branch claude/nebula-library-overhaul-2gg43z on newbound_nebula + newbound): FFI dylib packaging (root=nebula, tracked crate, no deps - attohttpc replaced by curl over system_call), all 14 commands re-authored via dev.code on a disposable (cert commands use nebula-cert -ca-key/-out flags so the CA key never leaves networks/<name>/; add_member's -group flag corrected to -groups - the old flag silently misfiled group membership; members keyed by peer name not path; one config template in save_config; systemd unit is Type=simple with Restart=on-failure), convert_legacy deleted, remove_member added behind the UI's delete button, descs everywhere, 17 orphaned Java-era records removed by reference-walk. Install story: dev.github.import + one restart + app activation, proven end to end on a fresh disposable including a real v1.9.5 release download and nebula-cert verify. NOT verified (owner hardware): live systemd ops, tun devices, real overlay traffic; publishapp intentionally not run so the owner's identity, not a scratch identity, signs the app republish.\",\n    \"tags\": \"nebula,overhaul,verification,keys\",\n    \"source\": {\n      \"doc\": \"this session's verification battery (47 checks) + import/update/remove wire tests, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786295588125\n  },\n  {\n    \"claim\": \"The nebula UI was rebuilt 2026-08-10 on the tokens.css kit (branch claude/nebula-library-overhaul-2gg43z, ea1c503, awaiting merge): all four controls' facets re-authored via patch_control_facet on a disposable; app.ui dependency dropped (own tabs/modal/status strip); owner-vs-joined gating from info.owner; outcomes read from data.status because the exec envelope is 'ok' even for failed object-returning commands. Traps hit and worth keeping: jQuery attr(name,null) does NOT remove an attribute (use removeAttr, or [hidden] never unhides); the server caches built jsapi per process, so facet js edits need an instance restart to serve (facet html/css serve fresh); mounting app:tokens via a hidden data-control div injects the kit css globally from any classic-mount app.\",\n    \"tags\": \"nebula,ui,frontend,kit,tokens,jsapi\",\n    \"source\": {\n      \"doc\": \"live Playwright verification on the disposable, 21 checks, 2026-08-10\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786320393147\n  }\n]\n","patch_id":"p6","time":1786321383758},{"author":"claude, nebula-v51-fold session 2026-08-10","cmd":"","facet":"memory","label":"memory: The v52 candidate (newbound_nebula branch, 7e54739 on top of ...","new":"[\n  {\n    \"claim\": \"The nebula library (mraiser/newbound_nebula, store v42) is four controls: `nebula` carries the app UI and all 14 rust commands; `lighthouse`, `network`, `member` are popup forms only. The 14 commands split into release management (info, install_release), CA/certs (create_network, add_member), rollout (join_network executed on the remote peer via the send_*(..., target) peer proxy, members), config rendering (save_config, build_config — duplicated YAML template), and systemd control (install/uninstall/start/stop/restart_service); convert_legacy is a dead stub.\",\n    \"tags\": \"nebula,architecture,controls\",\n    \"source\": {\n      \"doc\": \"read from data/nebula @ d0bcb0a, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476509\n  },\n  {\n    \"claim\": \"Nebula key doctrine: every private key lives under runtime/nebula/networks/<name>/ (gitignored by the repo), the CA key never leaves the owning instance, and member cert+key travel only as join_network parameters over the already-encrypted peer channel. The 2023 code has one wart to fix: create_network and add_member stage ca.key/certs through the process working directory (rename/copy dance) instead of using nebula-cert's -ca-key/-ca-crt/-out-crt/-out-key flags — a crash mid-command leaves a CA key in the checkout root.\",\n    \"tags\": \"nebula,security,keys,certs\",\n    \"source\": {\n      \"doc\": \"src/nebula/nebula/{create_network,add_member}.rs @ d0bcb0a\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476511\n  },\n  {\n    \"claim\": \"The nebula overhaul strategy (awaiting owner review) is docs/overhaul-strategy.md on branch claude/nebula-library-overhaul-2gg43z of mraiser/newbound_nebula: repackage on the newbound-agent FFI pattern (meta.json root=nebula, cargo ffi dylib, tracked generated crate), install via dev.github.import + one rebuild/restart, rewrite cert commands to use nebula-cert flags, dedupe the YAML template, drop attohttpc for system_call curl, delete the pre-split src/ tree and Java relics, fill desc everywhere, keep commands admin-only.\",\n    \"tags\": \"nebula,overhaul,strategy,packaging\",\n    \"source\": {\n      \"doc\": \"docs/overhaul-strategy.md, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476512\n  },\n  {\n    \"claim\": \"The nebula overhaul executed 2026-08-09 (branch claude/nebula-library-overhaul-2gg43z on newbound_nebula + newbound): FFI dylib packaging (root=nebula, tracked crate, no deps - attohttpc replaced by curl over system_call), all 14 commands re-authored via dev.code on a disposable (cert commands use nebula-cert -ca-key/-out flags so the CA key never leaves networks/<name>/; add_member's -group flag corrected to -groups - the old flag silently misfiled group membership; members keyed by peer name not path; one config template in save_config; systemd unit is Type=simple with Restart=on-failure), convert_legacy deleted, remove_member added behind the UI's delete button, descs everywhere, 17 orphaned Java-era records removed by reference-walk. Install story: dev.github.import + one restart + app activation, proven end to end on a fresh disposable including a real v1.9.5 release download and nebula-cert verify. NOT verified (owner hardware): live systemd ops, tun devices, real overlay traffic; publishapp intentionally not run so the owner's identity, not a scratch identity, signs the app republish.\",\n    \"tags\": \"nebula,overhaul,verification,keys\",\n    \"source\": {\n      \"doc\": \"this session's verification battery (47 checks) + import/update/remove wire tests, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786295588125\n  },\n  {\n    \"claim\": \"The nebula UI was rebuilt 2026-08-10 on the tokens.css kit (branch claude/nebula-library-overhaul-2gg43z, ea1c503, awaiting merge): all four controls' facets re-authored via patch_control_facet on a disposable; app.ui dependency dropped (own tabs/modal/status strip); owner-vs-joined gating from info.owner; outcomes read from data.status because the exec envelope is 'ok' even for failed object-returning commands. Traps hit and worth keeping: jQuery attr(name,null) does NOT remove an attribute (use removeAttr, or [hidden] never unhides); the server caches built jsapi per process, so facet js edits need an instance restart to serve (facet html/css serve fresh); mounting app:tokens via a hidden data-control div injects the kit css globally from any classic-mount app.\",\n    \"tags\": \"nebula,ui,frontend,kit,tokens,jsapi\",\n    \"source\": {\n      \"doc\": \"live Playwright verification on the disposable, 21 checks, 2026-08-10\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786320393147\n  },\n  {\n    \"claim\": \"Install-path doctrine, per the owner (2026-08-10): the peer-to-peer path (dev.dev.install_lib streaming over the peer channel, driven from the app's own UI) is THE install story for users' peers; dev.github.import is the developer path for the first instance only. A session initially replaced the broken P2P button with git-import guidance — wrong direction, corrected. The fix that made P2P work for FFI libraries is dev.dev.activate_lib: one shared activation step (initializer regen, in-place manifest mend for the builder's default Cargo.toml, unconditional crate build, host rebuild, 'RESTART:' answer) called by both install_lib and github.import. Verified on a P2P-shaped disposable (real dirs, no repo, no crate): activate → restart → all commands live.\",\n    \"tags\": \"nebula,p2p,install,doctrine,activate_lib\",\n    \"source\": {\n      \"doc\": \"the owner's correction in session + disposable verification, 2026-08-10\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786321383753\n  },\n  {\n    \"claim\": \"The v52 candidate (newbound_nebula branch, 7e54739 on top of the verbatim v51 baseline eea4ef2) folds the overhaul onto the wild lineage per the owner's four calls: service model matches v51 (supervised child process via start + timer-fired init reading botd.properties start=; systemd demoted to labeled legacy toggles; init/start record ids preserved untouched so the control's timer wiring holds - never delete+upsert a timer-referenced command), tokens.css remains the style contract, publish stays under the v51 identity with meta version left at 51 for his publish to bump to 52. Facts learned: v44->v51 drift was only init/start/members-enrichment/smol-css/attohttpc-bump - all 14 shared command signatures byte-identical, so cross-version peer calls are safe; OLD/ cert archives and members_HOLD dirs nest INSIDE a network folder and are invisible to info/members; the supervised model has no process-stop (the running flag only stops the log pumps) and info does not report supervised state - both offered to the owner as follow-ups, not built.\",\n    \"tags\": \"nebula,v51,v52,fold,service-model,timer\",\n    \"source\": {\n      \"doc\": \"fold verification on a v51-seeded disposable: 15 command checks + 11 UI checks green, init timer observed firing at boot, 2026-08-10\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786445411924\n  }\n]\n","old":"[\n  {\n    \"claim\": \"The nebula library (mraiser/newbound_nebula, store v42) is four controls: `nebula` carries the app UI and all 14 rust commands; `lighthouse`, `network`, `member` are popup forms only. The 14 commands split into release management (info, install_release), CA/certs (create_network, add_member), rollout (join_network executed on the remote peer via the send_*(..., target) peer proxy, members), config rendering (save_config, build_config — duplicated YAML template), and systemd control (install/uninstall/start/stop/restart_service); convert_legacy is a dead stub.\",\n    \"tags\": \"nebula,architecture,controls\",\n    \"source\": {\n      \"doc\": \"read from data/nebula @ d0bcb0a, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476509\n  },\n  {\n    \"claim\": \"Nebula key doctrine: every private key lives under runtime/nebula/networks/<name>/ (gitignored by the repo), the CA key never leaves the owning instance, and member cert+key travel only as join_network parameters over the already-encrypted peer channel. The 2023 code has one wart to fix: create_network and add_member stage ca.key/certs through the process working directory (rename/copy dance) instead of using nebula-cert's -ca-key/-ca-crt/-out-crt/-out-key flags — a crash mid-command leaves a CA key in the checkout root.\",\n    \"tags\": \"nebula,security,keys,certs\",\n    \"source\": {\n      \"doc\": \"src/nebula/nebula/{create_network,add_member}.rs @ d0bcb0a\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476511\n  },\n  {\n    \"claim\": \"The nebula overhaul strategy (awaiting owner review) is docs/overhaul-strategy.md on branch claude/nebula-library-overhaul-2gg43z of mraiser/newbound_nebula: repackage on the newbound-agent FFI pattern (meta.json root=nebula, cargo ffi dylib, tracked generated crate), install via dev.github.import + one rebuild/restart, rewrite cert commands to use nebula-cert flags, dedupe the YAML template, drop attohttpc for system_call curl, delete the pre-split src/ tree and Java relics, fill desc everywhere, keep commands admin-only.\",\n    \"tags\": \"nebula,overhaul,strategy,packaging\",\n    \"source\": {\n      \"doc\": \"docs/overhaul-strategy.md, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786292476512\n  },\n  {\n    \"claim\": \"The nebula overhaul executed 2026-08-09 (branch claude/nebula-library-overhaul-2gg43z on newbound_nebula + newbound): FFI dylib packaging (root=nebula, tracked crate, no deps - attohttpc replaced by curl over system_call), all 14 commands re-authored via dev.code on a disposable (cert commands use nebula-cert -ca-key/-out flags so the CA key never leaves networks/<name>/; add_member's -group flag corrected to -groups - the old flag silently misfiled group membership; members keyed by peer name not path; one config template in save_config; systemd unit is Type=simple with Restart=on-failure), convert_legacy deleted, remove_member added behind the UI's delete button, descs everywhere, 17 orphaned Java-era records removed by reference-walk. Install story: dev.github.import + one restart + app activation, proven end to end on a fresh disposable including a real v1.9.5 release download and nebula-cert verify. NOT verified (owner hardware): live systemd ops, tun devices, real overlay traffic; publishapp intentionally not run so the owner's identity, not a scratch identity, signs the app republish.\",\n    \"tags\": \"nebula,overhaul,verification,keys\",\n    \"source\": {\n      \"doc\": \"this session's verification battery (47 checks) + import/update/remove wire tests, 2026-08-09\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786295588125\n  },\n  {\n    \"claim\": \"The nebula UI was rebuilt 2026-08-10 on the tokens.css kit (branch claude/nebula-library-overhaul-2gg43z, ea1c503, awaiting merge): all four controls' facets re-authored via patch_control_facet on a disposable; app.ui dependency dropped (own tabs/modal/status strip); owner-vs-joined gating from info.owner; outcomes read from data.status because the exec envelope is 'ok' even for failed object-returning commands. Traps hit and worth keeping: jQuery attr(name,null) does NOT remove an attribute (use removeAttr, or [hidden] never unhides); the server caches built jsapi per process, so facet js edits need an instance restart to serve (facet html/css serve fresh); mounting app:tokens via a hidden data-control div injects the kit css globally from any classic-mount app.\",\n    \"tags\": \"nebula,ui,frontend,kit,tokens,jsapi\",\n    \"source\": {\n      \"doc\": \"live Playwright verification on the disposable, 21 checks, 2026-08-10\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786320393147\n  },\n  {\n    \"claim\": \"Install-path doctrine, per the owner (2026-08-10): the peer-to-peer path (dev.dev.install_lib streaming over the peer channel, driven from the app's own UI) is THE install story for users' peers; dev.github.import is the developer path for the first instance only. A session initially replaced the broken P2P button with git-import guidance — wrong direction, corrected. The fix that made P2P work for FFI libraries is dev.dev.activate_lib: one shared activation step (initializer regen, in-place manifest mend for the builder's default Cargo.toml, unconditional crate build, host rebuild, 'RESTART:' answer) called by both install_lib and github.import. Verified on a P2P-shaped disposable (real dirs, no repo, no crate): activate → restart → all commands live.\",\n    \"tags\": \"nebula,p2p,install,doctrine,activate_lib\",\n    \"source\": {\n      \"doc\": \"the owner's correction in session + disposable verification, 2026-08-10\"\n    },\n    \"confidence\": \"high\",\n    \"time\": 1786321383753\n  }\n]\n","patch_id":"p7","time":1786445411925}]},"id":"lgitmu19fe7541a59k0_patches","readers":[],"time":1786445411925,"username":"system","writers":[]}